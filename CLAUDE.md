# CsvPrintGokko プロジェクト固有ルール

## 依存関係更新ポリシー(サプライチェーン攻撃対策)

npm/PyPI/RubyGems等のパッケージエコシステムで、公開直後の正規パッケージが侵害されたり
悪性パッケージが混入したりする事例が相次いでいる(参考: LAC Watch
https://www.lac.co.jp/lacwatch/alert/20260522_004729.html )。
このリポジトリでは、依存関係(NuGetパッケージ、および将来npm等を導入した場合のパッケージ)を
更新・新規追加する際、以下の検証期間ルールを適用する。

- **通常の開発作業**: 対象バージョンの公開から**24時間以上**経過していることを確認してから取り込む。
- **本番ビルド/CI環境で使うバージョン**: 公開から**48〜72時間程度**経過していることを確認してから取り込む。
- 自動更新ツール(Dependabot等)を今後導入する場合も、即時マージではなく上記の確認期間を置く設定にする。
- パッケージのバージョンは常に明示的に固定し(ワイルドカード指定は使わない)、意図しないバージョンの
  自動追従を避ける。

理由: サプライチェーン攻撃(自己伝播型マルウェアの混入等)は公開直後に検出・報告されることが多いため、
一定の様子見期間を置くことで汚染バージョンを引き込むリスクを下げられる。

### 適用にあたっての確認方法
- NuGetパッケージ: [NuGet Gallery](https://www.nuget.org/)の該当パッケージページで「公開日」を確認する。
- npm等を将来使う場合: `npm view <package>@<version> time` 等で公開日時を確認する。
- 上記期間を満たさない最新バージョンへの更新提案があった場合は、その旨をユーザーに伝えたうえで
  期間が経過するまで見送るか、ユーザーの判断を仰ぐ。
